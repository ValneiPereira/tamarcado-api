# === Tá Marcado! API - Production Environment ===
# Copy this file to .env.prod and fill in the values:
#   cp .env.prod.example .env.prod

# PostgreSQL (local Docker container)
POSTGRES_DB=tamarcado
POSTGRES_USER=tamarcado
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here

# Redis (Upstash - free tier)
# Get your URL at https://console.upstash.com
REDIS_URL=rediss://default:YOUR_UPSTASH_PASSWORD@YOUR_HOST.upstash.io:6379

# JWT Secret (minimum 256 bits / 32 characters)
# Generate one: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_generate_a_strong_secret_key_minimum_32_chars

# CORS (comma-separated origins)
CORS_ALLOWED_ORIGINS=*
